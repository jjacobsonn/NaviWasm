import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// For React 17, use ReactDOM.render:
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);